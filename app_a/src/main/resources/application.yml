server:
  servlet:
    context-path: /app-a
  port: 8081
spring:
#  config:
#    import: "configmap:docker/app-a-config.yml"
  datasource:
#    url: jdbc:postgresql://localhost:5432/a_database
#    username: postgres
#    password: postgres
    url: "${DATABASE_URL}"
    username: "${DATABASE_USERNAME}"
    password: "${DATABASE_PASSWORD}"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
      database-platform: org.hibernate.dialect.PostgreSQLDialect
      globally_quoted_identifiers: true
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    console:
      enabled: true
item-service:
#  url: http://localhost:8082/app-b
  url: "${SERVICE_B_URL}"
